#include "wireless_sensor.h"

WirelessSensor::WirelessSensor(int r_, int c_, int x_, int y_):
    row(r_), col(c_), x(x_), y(y_)
{
    
}


int WirelessSensor::send_alert_to_base(char* alert_msg) {

}